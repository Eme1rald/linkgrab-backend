services:
  - type: web
    name: linkgrab-backend
    env: node
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y yt-dlp ffmpeg
      npm install
    startCommand: npm start
    autoDeploy: true
